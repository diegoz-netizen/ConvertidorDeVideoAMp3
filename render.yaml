services:
  - type: web
    name: mi-descargador-mp3
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    buildpacks:
      - url: https://github.com/heroku/heroku-buildpack-ffmpeg-latest.git